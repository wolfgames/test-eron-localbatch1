export const EVIDENCE_LIST_DEFAULTS = {
  /** Size of each item thumbnail (square, pixels) */
  thumbnailSize: 48,
  /** Vertical gap between rows */
  gap: 8,
  /** Label font size */
  labelFontSize: 12,
  /** Label text color */
  labelColor: 0xffffff,
  /** Alpha for found items */
  foundAlpha: 1.0,
  /** Alpha for unfound items */
  unfoundAlpha: 0.4,
  /** Tint applied to found item thumbnails */
  foundTint: 0xffffff,
  /** Tint applied to unfound item thumbnails */
  unfoundTint: 0x888888,
  /** Maximum visible height before scroll is enabled */
  maxHeight: 300,
  /** Scroll speed (pixels per wheel delta unit) */
  scrollSpeed: 20,
  /** Inner padding around the list */
  padding: 8,
};
