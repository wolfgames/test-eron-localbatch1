export const AMBILIGHT_DEFAULTS = {
  /** Blur filter strength applied to the glow sprite */
  blurRadius: 30,
  /** Glow alpha / brightness (0-1) */
  intensity: 0.6,
  /** Scale multiplier for the glow relative to source (1.15 = 15% larger) */
  glowScale: 1.15,
  /** Enable subtle breathing pulse animation */
  pulseEnabled: false,
  /** Pulse cycle duration in seconds */
  pulseSpeed: 0.8,
  /** Intensity variation during pulse (added/subtracted from base intensity) */
  pulseAmount: 0.1,
};
