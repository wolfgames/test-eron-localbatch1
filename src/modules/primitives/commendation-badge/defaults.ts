export const COMMENDATION_BADGE_DEFAULTS = {
  /** Whether to play entrance animation */
  animated: true,
  /** Delay before animation starts (seconds) */
  animationDelay: 0,
  /** Drop-slam animation duration (seconds) */
  dropDuration: 0.3,
  /** Drop-slam easing curve */
  dropEase: 'back.out(1.5)',
  /** Number of sparkle particles */
  sparkleCount: 8,
  /** Sparkle animation duration (seconds) */
  sparkleDuration: 0.6,
  /** Sparkle spread radius in px */
  sparkleSpread: 40,
  /** Scale at start of drop animation */
  initialScale: 2.0,
};
