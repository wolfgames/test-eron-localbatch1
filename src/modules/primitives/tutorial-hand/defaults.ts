export const TUTORIAL_HAND_DEFAULTS = {
  /** Resting scale of the hand sprite */
  restScale: 0.8,
  /** Scale during tap press */
  tapPressScale: 0.65,
  /** Y offset below target for intro slide-in */
  startOffsetY: 40,
  /** Fade-in duration in seconds */
  fadeInDuration: 0.4,
  /** Fade-out duration in seconds */
  fadeOutDuration: 0.3,
  /** Wait before first tap in seconds */
  waitBeforeTap: 0.3,
  /** Tap press duration in seconds */
  tapPressDuration: 0.15,
  /** Hold duration after press in seconds */
  tapHoldDuration: 0.1,
  /** Release duration in seconds */
  tapReleaseDuration: 0.15,
  /** Distance to back off after tap in pixels */
  backOffDistance: 20,
  /** Back-off duration in seconds */
  backOffDuration: 0.3,
  /** Wait between taps in seconds */
  waitBetweenTaps: 0.5,
  /** Slide back up duration in seconds */
  slideUpDuration: 0.3,
};
